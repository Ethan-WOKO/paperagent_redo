package com.yanban.core.research;

/** Metadata is server-attested; extracted Project content remains untrusted data. */
public enum TrustLabel {
    SERVER_ATTESTED_METADATA,
    UNTRUSTED_PROJECT_CONTENT
}
